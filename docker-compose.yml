version: '3'
services:
  flask:
    build: ./server
    container_name: carification-backend
    restart: on-failure
    volumes:
      - ./server:/server
    ports:
      - "5000:5000"
    command: python main.py